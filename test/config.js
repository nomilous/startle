describe('config', function () {
  it('supports process start opts as function');

  it('supports process stop opts as function');
  // puts sequence into run

  it('supports mixed agent start opts');
});
